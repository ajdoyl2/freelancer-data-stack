# PostgreSQL Configuration
# Used by Airbyte, Metabase, and Airflow for metadata storage
POSTGRES_PASSWORD=SecurePostgresPassword123!

# Neo4j Configuration
# Used by DataHub for graph database
NEO4J_PASSWORD=SecureNeo4jPassword123!

# DataHub Configuration
# Secret key for DataHub (minimum 32 characters required)
DATAHUB_SECRET=SuperSecureDataHubSecretKey123456789!

# Apache Airflow Configuration
# Web UI credentials (admin/admin by default)
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=airflow_admin_password_123!

# Airflow Fernet Key for encryption (generate new one for production)
# To generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=ZtE1T3BlbkFyZSVzNHR0aW5nVXBBbmRBaXJmbG93ITEyMw==

# Jupyter Configuration (leave empty for no token, or set a secure token)
JUPYTER_TOKEN=

# Home directory (automatically set to your user home)
HOME=/Users/ajdoyle
